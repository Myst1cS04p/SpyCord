# SpyCord
A simple plugin designed to log all commands run by the server, or by an op to a discord channel via a webhook. It also logs gamemode switches. This plugin is basically designed to get rid of op abuse accusations on small servers.
